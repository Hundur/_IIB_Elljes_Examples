CREATE COMPUTE MODULE Test
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		
		DECLARE fullDate TIMESTAMP '2020-02-04 00:00:00';
		DECLARE weekDay CHAR EXTRACT(DAYOFWEEK FROM fullDate);
		
		DECLARE X INT 1;
		SET X = 2;
		
		RETURN TRUE;	
	END;
END MODULE;